int abs(int a) {
    if (a < 0) {
        return 0-a;
    } else {
        return a;
    } endif
}

void main(void) {
    int i;
    int j;
    int sum;
	int arr[5];
	
    j = 3;
    i = abs(arr);
    j = abs(i, sum);
    bound = 5;
    output(bound);
    sum = 0;
    for (i = 1 + b; 2 < i; i = i + 1;) {
        j = j + 1;
        if (j < bound) {
        	sum = sum + 3;
        } else {
                sum = sum + 5;
        }
		endif
        output(sum);

    } 
    break;
    output(i);
}